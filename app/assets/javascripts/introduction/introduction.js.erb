APP.introduction = {
  index: function() {
    $('.background-slider').cycle({
      autoHeight: false,
      slides: '> div',
      speed: 800,
    });
    $('section .image-section').cycle({
      autoHeight: false,
      slides: '> div',
      speed: 800,
      fx: 'scrollHorz',
      pauseOnHover: true,
      swipe: true,
      swipeFx: 'scrollHorz',
    });
    $('.lazy').lazyload({
      effect: "fadeIn"
    });
    $('body').find('select, :radio, :checkbox, input[type=file]').cwicControl();
    if (Modernizr.svg) {
      $('#logo-container .logo').attr('src', '<%= asset_path('logo.svg') %>');
    }
  },
}
