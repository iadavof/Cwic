<% content_for :page_title do %>
<%= edit_title(@organisation_client) %>
<% end %>

<%= render 'form' %>

<%= button_link_back_to_show([@organisation, @organisation_client]) %>
<%= button_link_back_to_index([@organisation, OrganisationClient]) %>