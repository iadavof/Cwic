<% content_for :page_title do %><%= edit_title(@info_screen) %><% end %>

<% content_for :local_menu_navigation do %>
  <%= button_link_back_to_show([@organisation, @info_screen]) %>
  <%= button_link_back_to_index([@organisation, InfoScreen]) %>
<% end %>

<%= render 'form' %>
