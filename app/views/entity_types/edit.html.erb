<% content_for :page_title do %><%= edit_title(@entity_type) %><% end %>

<%= render 'form' %>

<%= button_link_back_to_show([@organisation, @entity_type]) %>
<%= button_link_back_to_index([@organisation, EntityType]) %>